# Security

Sync data is end-to-end encrypted: the encryption key is derived locally
from a user passphrase and never transmitted. The sync server (self-hosted
or hosted) only ever stores encrypted blobs. There is no server-side
passphrase recovery &mdash; a lost passphrase means lost synced data.

To report a vulnerability, contact github@lightmorphic.co.uk.
